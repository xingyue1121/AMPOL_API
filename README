This is the solution to Part 1: API Test, written in REST Assured(Java)

Please make sure Java(1.8 or above) and Maven are installed in your machine before executing the following steps.

Step 1: Retrieve the source code from repo
        git clone https://github.com/xingyue1121/AMPOL_API.git
Step 2: Build the source files
        /projectFolder  mvn clean compile package assembly:single
Step 3: Run the Java API test
        /projectFolder  java -jar target/API-0.0.1-SNAPSHOT-jar-with-dependencies.jar
           
After the test completes, you can see in the project folder a ResponseResult.xml file generated.
